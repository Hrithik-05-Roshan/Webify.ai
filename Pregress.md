### Client - to build the fronted
### Server - to build the backend

## Inside Server Folder :
- npm init : to initialize the package.json : type - module
- npm i express mongoose nodemon jsonwebtoken cookie-parser cors - required pckages intalled
- change the script in ```package.json``` to nodemon index.js to run the server & test to dev
- created dotenv put the variables like port and mongodb_uri 
- import it in index 
- connect the databse mongodb atlas : access from everywhere
- make a folder named config inside it db.js and here make a function connectdb using trycatch that will connect the databse to the server,exporting it, and import it in index.js and call the function to connect the database
- 
- 
- 
- 
- 
